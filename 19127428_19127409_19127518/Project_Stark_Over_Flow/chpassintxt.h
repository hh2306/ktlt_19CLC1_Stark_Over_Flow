#pragma once
#include <iostream>
#include <fstream>
#include "Projec.h"
void chpassintxt(member user, loginresult type,int x,int y, int z); // x = numbers of staffs, y = numbers of lectures, z= numbers of students
// if only chpass function return true; then we will process in the txt file
